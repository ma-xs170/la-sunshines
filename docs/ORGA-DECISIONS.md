# Décisions prises en autonomie

| # | Décision | Raison |
|---|---|---|
| D1 | Stripe Connect (déjà en place) remplace le formulaire IBAN en 4 étapes. Le RIB est saisi chez Stripe (identité, compte bancaire, justificatifs, statut « en cours de vérification / validé »). Aucun IBAN stocké ni affiché chez nous. | Exigence « IBAN jamais en clair » respectée par construction, moins de risque RGPD, pas de coffre à chiffrer. Versements toujours manuels. |
| D2 | Références : `ORG.` + 8 chiffres tirés avec `gen_random_bytes` (jamais séquentiels), index unique, boucle de relance en cas de collision ; `ADM.` idem sur `profiles`. Immuables (trigger qui refuse toute modification). | Anti-énumération. |
| D3 | Cycle de vie `en_attente → approuvé → suspendu` sur `organizers.account_status`. Les organisations existantes (THE MOUV) sont rétro-remplies en `approuvé`. Une organisation suspendue garde son accès en lecture mais ne peut plus rien publier ni vendre (appliqué côté serveur dès que les pages concernées existent). | Ne rien casser pour THE MOUV. |
| D4 | Nouveau CSS dans `app/organizer-shell.css` (importé par le layout organisateur), pas dans `globals.css`. | `globals.css` a des modifications non commitées d'une autre session. |
| D5 | Aucune nouvelle URL n'écrase une URL existante ; les pages actuelles (`/organisateur/participants`, etc.) restent servies. | « Ne casse aucune URL ». |
| D6 | Les migrations 010-013 restent à appliquer avant 014+. Je ne les applique pas sans `POSTGRES_URL_NON_POOLING`. | Base partagée. |
| D7 | Temps réel : (a) toute écriture du store ou de la billetterie appelle `revalidatePublicSite()` (`revalidateTag` + `revalidatePath('/', 'layout')`) ; les `revalidate = 60` des pages restent un simple filet de sécurité ; (b) stock public interrogé toutes les 2 s (cache CDN 1 s), en pause onglet masqué ; (c) tableau de bord d'évènement : sondage 2 s d'un point léger (`/live`, compteurs sans donnée personnelle), rechargement seulement si un chiffre change. | Supabase Realtime en `postgres_changes` exigerait des politiques RLS de lecture sur `tickets`/`orders` pour les membres d'organisation (aujourd'hui réservées au client et à l'admin) : je ne les ouvre pas sans validation. Le repli demandé (1-2 s, pause onglet masqué) est donc la solution retenue, sans canal à sécuriser. À reprendre avec des canaux Broadcast (sans donnée personnelle) si tu veux de l'instantané push. |
| D8 | En production, une modification de contenu éditorial (`/admin`) est un commit GitHub : la fiche publique est à jour au redéploiement qui suit (~1 min), pas avant, car le système de fichiers Vercel est en lecture seule. L'invalidation immédiate joue à plein pour tout ce qui est en base (billetterie, tarifs, stock). | Contrainte d'hébergement déjà documentée dans la mémoire du projet. |
| D9 | `next lint` n'est pas configuré dans ce dépôt (ESLint demande une configuration interactive) : les contrôles de phase sont `tsc --noEmit`, `next build`, `test:unit`, `test:db`. Je n'ajoute pas ESLint sans ton accord. | Ne pas modifier l'outillage sans besoin. |
| D10 | Entrées de menu non encore construites : affichées grisées avec la mention « Bientôt », non cliquables, jamais de page vide. Elles disparaissent au fil des phases. | Règle « zéro page vide déguisée ». |
| D11 | Les cases « viewer » du cahier n'existent plus : les rôles sont `owner / manager / staff` (migration 010). Un `staff` ne voit que Tableau de bord, Scan et le Centre d'aide. | Cohérence avec l'existant. |
| D12 | Vidéo du flyer : envoi direct navigateur → Vercel Blob (jeton serveur, formats et taille bornés), mesure et image d'aperçu faites dans le navigateur. Vidéo conforme (≤ 1080p, ≤ 60 i/s) : diffusée telle quelle (pas de ré-encodage). Vidéo 4K / > 60 i/s : enregistrée « Envoyée », l'affiche reste l'image ; la décision de transcodage (HEVC `libx265 -tag:v hvc1`, plafonds 1080p / 60 i/s, repli H.264 30 i/s) est écrite et testée dans `lib/videoRules.ts`, **mais aucun job ffmpeg ne tourne encore** (les fonctions Vercel n'embarquent pas ffmpeg de façon fiable ; un job externe gratuit type GitHub Actions est la voie, à brancher avec ton accord). | Ne pas promettre un traitement qui n'existe pas ; coût 0 €. |
| D13 | Lieux : coordonnées saisies (latitude / longitude) avec lien Google Maps généré ; pas de carte à marqueur déplaçable. | Une bibliothèque de carte ajoute une dépendance et des fournisseurs de tuiles : à décider. |
| D14 | Lineup côté organisateur : entrée « Bientôt ». Le lineup et les fiches artistes vivent dans le contenu éditorial (`data/content.json`, commit GitHub) réservé au panneau /admin ; l'ouvrir aux organisateurs demande une décision de migration. Le système existant (liens artistes, MAJUSCULES/gras, création auto) n'est pas modifié. | Ne pas régresser l'existant. |
| D15 | Texte des conditions par défaut = règlement du site, désormais dans `lib/rulesText.ts` (source unique de `/interdits`). Le rendu de `/interdits` reste identique. | Pas de duplication. |
| D16 | Phase 3 : les remboursements restent une action ADMIN (l'argent passe par Stripe, chemin critique) ; l'organisateur les consulte et les demande via le Support. Les codes promo sont gérés et contrôlés (`promo_preview`) mais NON appliqués au paiement Stripe tant que la billetterie n'est pas ouverte : toucher `reserve_tickets` et Stripe sans te consulter serait dangereux. Le formulaire de tarif existant (TiersPanel) est conservé tel quel (pas de simulateur 2 colonnes, pas de glisser-déposer, pas de « Décliner/Pack/Tarif privé »). « Liste d'entrée », « Imprimer des billets (lots PDF) » : « Bientôt ». | Prudence sur le chemin d'argent, périmètre. |
| D17 | Phase 4 : Audience, Acquisition, Tunnel de conversion, Canaux, Géographie, Performance : entrées « Bientôt » (aucune donnée de fréquentation ni d'acheteur n'est collectée ; pas de chiffres inventés). « Ventes » (matrice tarif × jour, chaleur jour × heure) et le récapitulatif financier sont réels. Les versements sont enregistrés à la main par un admin (`admin_record_payout`) ; aucun virement automatique. Les préférences de notification sont enregistrées mais aucun e-mail automatique n'est encore envoyé. | Honnêteté des chiffres, périmètre. |
| D18 | Espace de gestion admin dans `/admin/gestion/*` (compte Supabase admin actif requis, vérifié à chaque page et action ; le mot de passe historique `ADMIN_PASSWORD` du panneau `/admin` reste inchangé et ne donne aucun accès ici). Retirer plus tard l'accès legacy : supprimer `sun_admin` de `lib/adminAuth.ts` une fois tout migré. Les admins existants sont devenus « super » ; l'A2F est prête (colonne `mfa_required`, MFA TOTP de Supabase) mais non imposée. Le mot de passe provisoire (20 caractères, aléatoire) n'existe que dans l'e-mail ; avec l'expéditeur `onboarding@resend.dev`, Resend n'envoie qu'à ta propre adresse tant que le domaine n'est pas vérifié : l'admin est alors créé « Invitation non envoyée » avec le bouton « Renvoyer ». | Exigences 6.1. |
| D19 | Recherche globale : `admin_global_search` (SQL), index trigramme si `pg_trgm` est disponible, sinon balayage (volumes actuels minuscules). Les recherches par e-mail / téléphone sont journalisées (type seulement). Les numéros de tickets support (TK.) sont ajoutés avec la Phase 6. | Vie privée. |
| D20 | Support : chat par interrogation toutes les 2 s (pause onglet masqué), pas de Supabase Realtime (même raison que D7). Pièces jointes en Vercel Blob PRIVÉ, relues seulement par `/api/support/file` après contrôle d'accès au ticket. Le support n'est ouvert qu'aux rôles owner / manager (le staff n'a pas de support). Les réponses rapides existent en base et sont utilisables ; leur création se fait par l'API/SQL (`support_quick_replies`), sans écran dédié. L'accusé de réception par e-mail est envoyé à l'auteur ; les e-mails « nouvelle réponse » ne sont pas encore branchés. | Périmètre. |
| D21 | Pages publiques d'organisateurs et bloc « Organisé par » : servis seulement si Supabase est configuré ET si le mode public n'est pas « bizouk » (`lib/publicOrganizer.ts`) ; sinon 404 / rien. Le calendrier ne montre que les évènements de billetterie ayant une session reliée à un lieu (région) ; les fiches purement éditoriales (`content.json`) n'y figurent pas. Les e-mails « nouvel évènement » : opt-in et désabonnement 1 clic sont en place, l'envoi lui-même n'est pas branché. | Production protégée. |

## Décisions du mode absence (2026-09-21)
- Lint : `npm run lint` n'est pas configuré (Next demande un assistant interactif) : je ne l'ajoute pas ; contrôle de types par `tsc --noEmit`, tests unitaires, tests SQL, e2e, build et Playwright.
- Merge sur main : via un worktree temporaire issu de origin/main (le worktree `la-sunshines-merge` contient des changements d'une autre session, on n'y touche pas).
- Mode public : la base contient `ticketing_mode = native` (réglé depuis l'admin le 2026-09-20 20:12) alors que la consigne dit « bizouk ». Je ne le modifie PAS ; à confirmer au retour.
- Code Bizouk (E) : jamais injecté. `lib/bizoukEmbed.ts` refuse tout ce qui n'est pas Bizouk (scripts en ligne, gestionnaires, javascript:, autres domaines, http, identifiants dans l'URL), n'extrait que l'identifiant numérique, et `BizoukWidget` REGÉNÈRE l'iframe (attributs fixes + sandbox). Pas de CSP `frame-src` globale : la page évènement embarque déjà d'autres iframes (cartes) et une CSP partielle les casserait ; la régénération côté serveur est le contrôle principal. Les codes déjà présents dans le code (lib/bizouk.ts) passent par le même filtre.
- Évènements créés en ligne (E) : créés en BROUILLON (`ticketed_events` + `event_details.title` + lieu + session), capacité provisoire 100 (à régler dans les tarifs), aucun n'est public : la publication reste une action séparée (bouton « Publier » non fourni, étape « validation avec l'équipe » via le support). Pas de page publique /editions pour ces évènements tant qu'ils ne sont pas rattachés à une édition (limite connue, à traiter avec la publication).
- SIRET THE MOUV : mis à jour en base (10425394300013 → 10665995600010) et journalisé (`organizer.siret_fix`). Les pages publiques /mentions-legales et /cgv (et docs/billetterie/LEGAL.md) contiennent encore l'ancien SIRET : NON modifiées (texte légal), à confirmer au retour.
- Bouton « Continuer » des assistants : `onMouseDown` empêche la perte de focus ; sinon la validation au blur décalait la mise en page entre l'appui et le relâchement et le clic était perdu (bug trouvé par le test Playwright).
