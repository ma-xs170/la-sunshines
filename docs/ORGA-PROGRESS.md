# Suivi d'avancement (à relire au début de chaque phase)

Branche : `feat/organisateur-ui`. Migrations appliquées sur la base réelle : 001 → 020 (021 = tarifs gratuits, à appliquer).
Méthode : sauvegarde JSON `~/sunshines-backups/` → migration → verify → build/tsc/tests → commit → push → (phase verte) merge main.
Scripts hors dépôt (scratchpad) : `db.mjs` (connexion, lit `.env.local`), `backup.mjs`, `apply.mjs`, `verify.mjs`.

- [x] Phase 0 : audit et décisions
- [x] Phase 1 : références ORG/ADM, menu latéral, temps réel (migration 014 appliquée)
- [x] Phase 2 : pages évènement (sauf Lineup organisateur et job de transcodage 4K : voir DECISIONS D12-D14)
  - [x] 2a libs pures + tests
  - [x] 2b migration 015 APPLIQUÉE sur la base réelle + tests SQL + verify
  - [x] 2c pages organisateur : Description, Lieux, Sessions, Formulaires, CG, RGPD, Décliner le flyer
  - [x] 2d affichage public dresscode couleurs + vidéo flyer (inerte tant que rien n'est saisi)
- [x] Phase 3 (migration 016 APPLIQUÉE) : commandes + détail, remboursements (lecture), historique des scans, invitations (envoi + suivi), codes promo (application au paiement NON branchée). Non faits : simulateur de prix / formulaire de tarif 2 colonnes, glisser-déposer, liste d'entrée, impression de lots PDF, messages en masse (existant conservé), voir DECISIONS D16
- [x] Phase 4 (migration 017 APPLIQUÉE) : cartes de ventes du tableau de bord, finance (récap, versements manuels, CSV), statistiques Ventes (matrice + chaleur), préférences de notification. Non faits : onglets Audience/Acquisition/Tunnel (pas de mesure de fréquentation), envoi e-mail des notifications, formulaire compte de versement (Stripe Connect à la place, D1), D17
- [x] Phase 4bis-A FAITE (migration 022 APPLIQUÉE le 2026-09-21, sauvegarde ~/sunshines-backups/avant-22, down/022_down.sql, tests/022_verify.sql tous ✓) : table `event_links` ; 7 évènements rattachés à THE MOUV (6 éditions + test-billetterie marqué test) ; compte mathxs.170@gmail.com déjà admin (ADM.29383836), désormais super-admin + OWNER de THE MOUV, aucun autre compte touché ; affichage : Mes évènements (onglets À venir / Passés / Brouillons / Archives, « Vendu via Bizouk — statistiques non disponibles ici »), liste sous le calendrier (organisateur + admin), page publique /organisateurs/the-mouv et bloc « Organisé par » (fusion lib/eventLinks.ts). Nouveaux évènements créés dans /admin/contenu : PAS rattachés automatiquement (à faire à la main en base / futur écran). Demande initiale : lier les évènements passés à THE MOUV
  1. Compte admin `mathxs.170@gmail.com` : vérifier dans Supabase Auth. Existe → `profiles.role='admin'`, référence ADM. attribuée, membre OWNER de THE MOUV (ORG.78973257). Ne modifier AUCUN autre compte. N'existe pas → ne rien créer, demander à l'utilisateur de s'inscrire sur /inscription avec cet e-mail, puis promouvoir sur confirmation.
  2. Rattacher TOUS les évènements (éditions statiques + data/content.json, La Nuit des Ombres, tous les passés, évènement de test marqué « test ») à THE MOUV. Migration additive et idempotente (table de liaison slug → organisation, ou organization_id si la table existe déjà). Aucune suppression, aucune modification du contenu éditorial, ne pas toucher data/content.json.
  3. Affichage : « Mes évènements » (À venir / Passés / Brouillons), calendrier, page publique THE MOUV (à venir / passés, artistes et DJs déjà programmés), bloc organisateur en bas de chaque page évènement (« THE MOUV — Suivre | Voir les évènements »).
  4. Passés vendus ailleurs (Bizouk) : AUCUN chiffre inventé, afficher « Vendu via Bizouk — statistiques non disponibles ici » + infos éditoriales seulement.
  5. État des lieux avant/après, sauvegarde JSON hors dépôt, fichiers `_down.sql` et `_verify.sql`, puis confirmer le nombre d'évènements rattachés.
- [x] Phase 4bis-B FAIT (code : build + tsc + 75 tests unitaires verts ; vérification visuelle avec un vrai compte admin à faire) : cadre `app/admin/layout.tsx` + `AdminShell` (mêmes classes/CSS que l'organisateur, menu plat), nouveau tableau de bord `/admin`, ancien panneau déplacé en `/admin/contenu` (`?onglet=`, intégré sans double menu ; `/api/admin/grant` pose le cookie historique aux admins actifs), `/admin/gestion/audit` et `/admin/gestion/reglages`, redirections `/admin?edit=` → `/admin/contenu?edit=`, `/admin/gestion` → `/admin`. Sans compte admin Supabase : l'ancien accès par mot de passe fonctionne comme avant (pas de cadre). Demande initiale (après 4bis-A) : /admin au même niveau que /organisateur
  - Même DA/layout que /organisateur (menu latéral PLAT pleine hauteur, jamais d'ovale), mêmes cartes, tableaux, états vides, responsive.
  - Menu : Tableau de bord · Organisateurs · Évènements · Administrateurs (super-admin seulement) · Support (Tous, Problème technique, Gestion du compte, Argent & paiement, Demandes de nouveautés, Autre, Mes tickets, Fermés) · Calendrier · Billetterie (mode Bizouk/interne, frais) · Contenu du site (tout l'ancien panneau : éditions, artistes, programme, actualités, règlement, infos) · Journal d'audit · Réglages.
  - Tableau de bord : organisateurs (en attente d'approbation, actifs), évènements à venir, tickets support non pris en charge, ventes plateforme, dernières actions.
  - Toutes les fonctions actuelles de /admin restent accessibles et fonctionnelles ; redirections des anciennes URLs.
- [x] Phase 5 (migration 018 APPLIQUÉE) : administrateurs multiples (création + mot de passe par e-mail, changement obligatoire, verrouillage 5 échecs, désactivation), organisateurs (liste, fiche à onglets, approbation/suspension/contact), transfert atomique, vue globale des évènements, recherche globale Cmd+K (/admin/gestion). Non fait : A2F imposée (colonne prête), création auto de la page publique à l'approbation (Phase 7)
- [x] Phase 6 (migration 019 APPLIQUÉE) : tickets TK.XXXXXX, chat (sondage 2 s), pièces jointes privées, ajout par ORG, prise en charge atomique, notes internes, réponses rapides (lecture + API), bouton d'aide flottant, /admin/gestion/support. Non fait : interface de création de réponses rapides, e-mails de notification de réponse
- [x] Correctif 404 / accès refusé : app/not-found.tsx (« Page introuvable », FR, boutons accueil / mon espace), app/forbidden.tsx (« Accès refusé », via forbidden() + experimental.authInterrupts) pour les refus de rôle (admin, scan, billetterie, /organisateur sans organisation, évènement sans accès organisateur). Données d'un autre organisateur : toujours 404. Tous les liens des menus organisateur/admin vérifiés (aucun 404) ; entrées sans page = « Bientôt » sans lien. Redémarrer le serveur de dev après le changement de next.config.mjs.
- [x] Phase 7 (migration 020 APPLIQUÉE) : pages /organisateurs/[slug] (auto à l'approbation, avatar aux initiales, JSON-LD, sitemap), bloc « Organisé par » sur les fiches, Suivre (opt-in e-mail, désabonnement 1 clic), édition de la page (logo/bannière), calendrier régional (mois/semaine/liste, conflits) organisateur + admin. Inerte tant que le mode public est « bizouk ». Non fait : envoi effectif des e-mails « nouvel évènement »
- [x] Vérification finale (tests unitaires + SQL verts, migrations 014-020 appliquées, main mergé acc1c12) + ORGA-RAPPORT.md. Parcours e2e complet non rejoué (voir rapport).
- [x] Phase 8 : tarifs GRATUITS (migration 021, à appliquer sur la base réelle — NON appliquée) : prix 0 accepté, 0,01–0,49 € refusé (message Stripe) ; commande à 0 € = aucune session Stripe (`reserve_free_order` : réservation du stock + `fulfill_order` en UNE transaction, billets QR + e-mail PDF immédiats, bouton « Réserver gratuitement », redirection vers /commande/succes) ; panier mixte = Stripe n'encaisse que le payant (lignes à 0 € exclues), frais % + fixes nuls si total 0 ; anti-abus : e-mail confirmé (`auth.users.email_confirmed_at`), plafond par compte `ticket_tiers.max_per_account` (défaut 2) + par commande, limitation de fréquence dédiée (5/10 min par compte, 20/10 min par IP), même verrou de stock ; badge « Gratuit » (tarifs, panneau public, total, billet PDF, e-mail, listes/commandes), colonne « Type » Gratuit/Payant dans les exports CSV, compteur gratuits/payants dans les stats ; remboursement refusé sans appel Stripe (409), `org_finance` ignore les commandes à 0 €. Tests : `supabase/tests/021_free_tickets.sql` (achat valide, sans stock, mixte, 0,30 refusé, anti-abus), `tests/unit/free-tickets.test.mjs`, e2e `s13` (22 contrôles). Vérifié en local : tarif à 0 € via l'API réelle. Le banc e2e s12 a 4 échecs déjà présents sur HEAD avant cette phase (pages /organisateur/analyse pour staff), sans lien avec les tarifs. À faire avant mise en ligne : sauvegarde JSON, appliquer 021, vérifier. Non fait : l'application des codes promo reste non branchée (Phase 3) ; le simulateur de prix admin n'existe pas encore.

## Demandes C à F (2026-09-21) : ordre de travail
- [x] C) Inscription organisateur en 5 pages (/devenir-organisateur : structure, responsable, pièces, activité, envoi ; brouillon local, validation champ par champ, pièces en stockage privé avec contrôle du contenu) + consultation par les admins (fiche organisateur > onglet « Dossier d'inscription », chaque ouverture journalisée). Migration 023 APPLIQUÉE le 2026-09-21. L'organisation est créée EN ATTENTE (OWNER = l'auteur).
- [x] D) Recherche globale admin : faite (Phase 5, `GlobalSearch`, Cmd+K) et branchée dans le cadre /admin (4bis-B).
- [x] E) FAIT le 2026-09-21 (migration 024 appliquée, main `7304f82`) : Création d'évènement en 3 étapes (/organisateur/evenements/nouveau) + code Bizouk sécurisé + SIRET THE MOUV 10665995600010 (journalisé) : après C.
- [x] F) Checkpoint 1 poussé sur origin/main (`7304f82`), migrations 022 à 025 appliquées, évènement de test supprimé, SIRET corrigé. Mode absence : tag `pre-orga-2026-09-21` posé sur origin/main (cf50a79) ; push continu ; merge sur main seulement si tsc + tests + build (dossier séparé) + Playwright sont verts ; mode public reste « bizouk » ; aucun e-mail hors mdn.productions170@gmail.com ; test complet automatisé puis suppression de l'évènement de test `test-billetterie` (sauvegarde, liste exacte, transaction, contrôle des comptages, retrait de SON entrée dans data/content.json seulement).

### Push du 2026-09-21 (`7304f82` sur main)
- En ligne : 4bis-A/B, C, D, E, promo au paiement (025), correctif `authInterrupts`, bancs e2e s14/s15/s16.
- Reste : migration 021 (billets gratuits) non appliquée sur la base réelle ; publication d'évènements créés en ligne ; échecs e2e antérieurs (s5, s9–s12) à réaligner ; e-mails automatiques.
- À ouvrir sur le téléphone : /admin, /organisateur/evenements/nouveau, /devenir-organisateur, /organisateur/evenements, /organisateurs/the-mouv, /editions/la-nuit-des-ombres.

### 2026-09-21 (suite) : décisions de Mathis appliquées
- Mode public `ticketing_mode` repassé sur **bizouk** (audit_log `setting.update` native → bizouk, acteur admin).
- Migration **021 appliquée** (relue : additive, tables vides ; sauvegarde `~/sunshines-backups/avant-021` ; `021_verify.sql` tout vert ; s13 / s16 / s3 verts en local).
- **SIRET THE MOUV = 10665995600010** aligné : base (journal `organizer.siret_fix`), `/mentions-legales`, `/cgv`, `docs/billetterie/LEGAL.md`, graine de la migration 009, tests. Le SIRET de LAWCY MUSIC (107 145 534 00015) est inchangé.
- Vercel : `https://la-sunshines.vercel.app` sert déjà le code de `main` (routes de ce lot présentes) ; projet exact / lien Git non lisibles via l'API (voir rapport à Mathis).
- Publication d'évènements créés en ligne : étapes restantes documentées dans ORGA-RAPPORT.md (non construites).

### 2026-09-21 (soir) : e2e verts + publication par l'organisateur (main `cebf500`)
- **E2E : suite complète verte (s3 → s17, 15 scénarios).** Causes des anciens échecs : (a) VRAI bug : un `<Suspense>` autour du cadre organisateur / admin faisait répondre 200 aux `notFound()` et `redirect()` des pages (soft-404, redirections invisibles côté HTTP) → retiré ; (b) `/organisateur` d'un compte sans organisation renvoyait un 403 sans issue → redirection vers `/devenir-organisateur` ; (c) tests cassés : texte « Accès refusé » présent dans le flux RSC de toutes les pages (statuts 403 vérifiés à la place), `innerText` en capitales (CSS), sélecteur de cloche renommé (`oside__badge`), `<head>` du site citant « La Nuit Des Ombres », lien `/admin?edit=` devenu redirection ; (d) banc : `next dev` du banc partageait `.next` avec le `next dev` de l'utilisateur (corruption aléatoire) → dossier `.next-e2e`.
- **Évènement de test** : supprimé de la base (sauvegarde `~/sunshines-backups/avant-suppression-test-billetterie`) ; rien dans `data/content.json` (défini dans le code, actif seulement en mode test hors production) ; `/editions/test-billetterie` → 404 en ligne.
- **Publication** (migration 026 appliquée) : voir ORGA-RAPPORT.md ; s17 (42 contrôles) + SQL 026 + unitaires.
- URLs téléphone : /admin/gestion/publications, /organisateur/evenements/<slug> (panneau « Publication »), /organisateur/evenements/<slug>/visuel, /editions/<slug> après validation.
## Page super-admin « Clients » (2026-09-21) — branche `feat/admin-clients`, worktree `../la-sunshines-clients`
Travail dans un worktree séparé (une 2e session tourne sur les ports 3130 / 543xx) : banc e2e décalé par `E2E_PORT_OFFSET=100`, `test:db` par `TESTDB_PORT=54439`, build dans `NEXT_DIST_DIR=.next-build`.

### Bloc 1 — audit : ce qui existait / ce qui manquait
| Donnée | Où | État |
|---|---|---|
| Prénom, nom, téléphone, rôle | `profiles` (first_name, last_name, phone ≤ 25, role customer/staff/admin) | existait |
| E-mail | `auth.users.email` seulement | **copié dans `profiles.email`** (synchronisé par trigger) |
| 2e téléphone | — | **`profiles.phone2`** |
| Date de naissance | — (seul `orders.guardian_consent_at` pour les mineurs) | **`profiles.birth_date`** (vide = « Non renseigné ») |
| Statut du compte | — (`auth.users.banned_until` seul) | **`profiles.account_status` (active/suspended/anonymized) + `status_reason` + `anonymized_at`** |
| Inscription / dernière connexion | `auth.users.created_at` / `last_sign_in_at` | existait (lu par la fonction de fiche) |
| Référence de compte | admins : `ADM.xxxxxxxx` ; clients : aucune | **`CLI.` + 10 hex de l'id**, calculée (aucune colonne) |
| Commandes, billets, évènements | `orders.user_id` → `order_items` (snapshot titre/date/tarif) → `tickets` (`reference` LS-XXXXXX) ; `ticketed_events.starts_at` | existait : à venir / passés = commandes payées ou partiellement remboursées selon `starts_at` |
| Autorisations parentales, consentements | `orders.guardian_consent_at`, `orders.terms_accepted_at/terms_version`, `order_consents` (texte exact accepté) | existait, rien d'autre à inventer |
| Droits admin délégués | `admin_accounts` (level super/admin) sans permissions | **`admin_accounts.permissions`** (`clients.lire`, `clients.modifier`, vides par défaut) |
| Journal | `audit_log` (entity/entity_id/before/after/meta) | réutilisé : entity `customer`, actions `customer.view/update/suspend/reactivate/anonymize/export/password_reset/sessions_revoked`, `customers.export` |

Migration **027** (`20260921000100_admin_clients.sql`) : ADDITIVE, `down/027_down.sql`, `tests/027_verify.sql`, `tests/027_admin_clients.sql` (branché dans `npm run test:db`). Sécurité en base : toutes les fonctions `admin_*` sont SECURITY DEFINER, exécutables par `service_role` seul, et revérifient le rôle à chaque appel (`_assert_clients` : super-admin actif, ou permission explicite) ; colonnes sensibles de `profiles` retirées du SELECT direct de `authenticated`.
Recherche : `norm_text` (sans accents ni casse) + `phone_key` (0690…, +590 690… se retrouvent) + colonne `search_text` maintenue par trigger + index trigramme (GIN). Compteurs d'évènements calculés sur les 20 lignes de la page seulement (pas de N+1).
Migration **NON appliquée** sur la base réelle : l'accès à la base de production a été refusé par les permissions de la session (voir rapport). Le code reste inerte tant que la fonction `admin_clients_access` n'existe pas (entrée de menu masquée, aucune page cassée).
### Bloc 2 — liste, recherche, pagination (2026-09-22)
- `/admin/clients` (entrée « Clients » dans le menu admin, `lib/admin/menu.ts`) : tableau 20 par page, colonnes Prénom / NOM (majuscules) / E-mail / Téléphone(s) / Naissance (+ âge) / À venir / Passés / Inscrit le / Statut. Clic sur une ligne (hors lien/bouton) → fiche `/admin/clients/[id]` (page encore à faire, bloc 3).
- Recherche, filtres, tri, pagination et total **côté serveur** (`admin_list_customers` de la migration 027) : jamais recalculés dans le navigateur. Tout est dans l'URL (`?q=&role=&statut=&avenir=1&mineurs=1&tri=&sens=&page=`), donc liens partageables et bouton retour fonctionnels (`lib/admin/clients/query.ts`, testé).
- Barre de recherche : anti-rebond 300 ms, minimum 2 caractères (message d'aide sinon), bouton « effacer », retour automatique à la page 1, squelette de chargement, message « Aucun résultat pour … », état d'erreur avec « Réessayer ».
- Pagination « Précédent 1 2 3 4 5 … 67 Suivant » : fonction pure `pageWindow` (`lib/admin/clients/pagination.ts`), testée unitairement sur tous les cas demandés (1, 2, 5, 6, 67 pages ; début, page 4, milieu, avant-dernière, dernière). `aria-current="page"` sur la page active, boutons Précédent/Suivant désactivés aux extrémités. Mobile (390 px) : le tableau devient une liste de cartes ; la pagination affiche une **vraie** fenêtre réduite (rayon 1, recalculée — pas un chiffre simplement masqué en CSS, ce qui cassait la suite 1 2 3… : bug détecté et corrigé pendant les tests Playwright).
- Export CSV (super-admin uniquement) : `/api/admin-clients/export`, UTF-8 avec BOM, protection anti-injection de formules (apostrophe devant `= + - @`), avertissement RGPD obligatoire avant téléchargement (case « inclure les mineurs » décochée par défaut), export journalisé côté base sans le texte recherché.
- Sécurité : page/API en `Cache-Control: no-store`, `X-Robots-Tag: noindex`, `Referrer-Policy: no-referrer` (`next.config.mjs`), non-autorisé → 404 (page **et** API), aucune donnée dans la réponse. Mesure d'audience (Vercel Analytics/Speed Insights, GA4) explicitement coupée sur tout `/admin` (`components/Metrics.tsx`) : les URL de la page Clients portent la recherche (nom, e-mail, téléphone) et ne doivent jamais atteindre un outil de mesure tiers.
- Droits délégués : entrée de menu et page visibles seulement si `admin_clients_access` répond `lire=true` (super-admin, ou permission `clients.lire`/`clients.modifier` accordée) ; tant que la migration 027 n'est pas appliquée en prod, la fonction échoue proprement et le menu masque l'entrée (`lib/admin/shell-data.ts`).
- Tests : unitaires (pagination, téléphones, CSV, formats, URL de filtres — 127 passent), Playwright `tests/e2e/s18.mjs` (404 pour client/organisateur/staff/admin non autorisé sur la page **et** l'API ; admin avec `clients.lire` seul : lecture OK, export 404 ; 20 lignes par page ; recherche accents/casse/nom complet/e-mail/téléphones équivalents/tentative d'injection SQL ; recherche jamais journalisée ; capture 1440 px et 390 px, aucune bande noire, menu plat). Comptes de test `@clients-e2e.local` créés puis supprimés par le script.
- Toujours inerte en production tant que la migration 027 n'est pas appliquée sur la vraie base (accès refusé à la session, voir bloc 1).
- Vérifié en ligne (la-sunshines.vercel.app) après le push : `/admin/clients` et `/api/admin-clients` → 404 propre pour un visiteur non connecté (`Cache-Control: no-store`, `X-Robots-Tag: noindex, nofollow`, `Referrer-Policy: no-referrer`), export → 404, accueil et `/admin` inchangés (200 / redirection normale).

## Série 2 (2026-09-21) : ordre de travail
Consigne : pousser après chaque bloc ; merge sur main seulement si tsc + tests (unitaires, SQL, e2e) + build (dossier séparé) + Playwright sont verts ; mode public « bizouk » inchangé ; aucune migration destructive, sauvegarde JSON avant chaque migration ; décisions dans ORGA-DECISIONS.md (D30+). Travail dans le worktree `la-sunshines-orga2` (branche `feat/orga-serie-2`), car une autre session modifie `la-sunshines-project`.
- [x] 1) Tableau de bord d'évènement : « Voir l'évènement » (public si publié, sinon aperçu privé `/organisateur/evenements/<slug>/apercu`, badge « Aperçu »), carte « Lien public de l'évènement » + « Copier le lien » (« Copié ! »), lien d'aperçu pour les brouillons. Mergé sur main (`f0f210e`).
- [x] 2) Cartes de statistiques (4 cartes + « Remplissage ») uniquement sur le tableau de bord d'évènement et la page Statistiques > Vue d'ensemble ; retirées des onglets Participants / Tarifs / Scan et de la fiche organisateur admin. Jauge PAR TARIF (vendus / stock, %, restantes, chiffre d'affaires, statut En vente / Épuisé / Fermé / À venir, alerte stock bas ou épuisé) : tableau de bord, onglet Tarifs, Vue d'ensemble ; mise à jour toutes les 2 s. Mergé (`f0f210e`).
- [x] 4) Bande noire supprimée : la règle globale `footer{background:#14100C}` s'appliquait au pied de page du back-office ; remplacée par `BackFooter` (© La Sunshines · CGU · Confidentialité · Contact) dans /organisateur et /admin. Mergé (`f0f210e`).
- [x] 3) Plus aucune entrée « Bientôt » : migration 030 + pages réelles (Frais et paiement, Lineup, Membres et rôles, Liste du staff, Présences, QR de connexion, Liste d'entrée, Message aux participants, Renvoyer les billets, Imprimer des billets, Médias, Statistiques Vue d'ensemble / Audience / Acquisition / Tunnel / Participants / Canaux / Géographie / Performance, Rôles de l'évènement) ; entrées hors périmètre supprimées (Marketing / liens de suivi, Photos). Menu vérifié par Playwright (organisateur ET admin, desktop + mobile, 85 pages au total) : zéro « Bientôt », zéro 404, zéro page vide. Corrigé au passage (D38-D40) : `org_orders`/`org_order_detail`/`org_finance` mal déclarées `stable` en SQL (écriture d'audit bloquée par PostgREST) ; débordement horizontal mobile sur `.org-table` (en-têtes masqués) et `.org-filters` (select large) ; `.admin-form__actions` sans retour à la ligne (`/admin/billetterie` entre autres) ; test e2e `s5` obsolète après une restriction de sécurité légitime de l'autre session (aucun bug applicatif). tsc + 112 tests unitaires + SQL (`test:db`) + e2e complet (s3-s18, 970+ contrôles) + build de production (dossier séparé) : tous verts.
- [x] 5) E-mails d'administrateurs + diagnostic e-mail dans /admin. Corrigé au passage (D41) : `lib/mail.ts` ignorait `MAIL_FROM` (expéditeur toujours `onboarding@resend.dev`, même une fois la variable réglée) — bug touchant TOUS les e-mails passant par `sendMail()` (invitation admin, ticket support, transfert d'évènement, formulaire de contact, assistant). Corrigé + centralisé (`fromAddress()`, un seul endroit). Gabarit de marque (`mailLayout`) appliqué à l'invitation admin, l'accusé de réception support et le transfert d'évènement (étaient en HTML brut). E-mail d'invitation : identifiant, mot de passe provisoire, lien de connexion, changement obligatoire — jamais loggé, jamais renvoyé au navigateur. Réglages > E-mails (`MailDiagPanel`) : état RESEND_API_KEY/MAIL_FROM, vérification du domaine chez Resend (`resend.domains.list()`), bouton « Envoyer un e-mail de test à mon adresse », erreurs lisibles (domaine non vérifié, clé invalide, limite atteinte) ; « Renvoyer l'invitation » existait déjà (page Administrateurs). Découvert manquant et ajouté : aucun e-mail n'était envoyé à l'approbation/suspension d'une organisation (D42 : pas de flux de « refus » distinct, seulement approuver/suspendre — voir ORGA-DECISIONS). Banc e2e étendu (`tests/e2e/infra.mjs`) : `POST /auth/v1/admin/users` (création de compte) et `/domains` (Resend) simulés, nécessaires pour tester ce bloc de bout en bout — absents jusqu'ici, jamais exercés par les tests précédents. Nouveau `tests/e2e/s20.mjs` (33 contrôles) + `tests/unit/mail.test.mjs`. tsc + 131 tests unitaires + SQL + e2e complet (s3-s20, 20 scénarios, 1450+ contrôles) + build de production : tous verts.
- [ ] 6) Fonctions admin en plus
- [ ] 7) Livraison : rapport de fin de bloc

### Bloc 3 — fiche client, édition, permissions déléguées (2026-09-22)
- `/admin/clients/[id]` : en-tête (nom, badges Mineur/statut/rôle, référence, inscrit le, dernière connexion) + 6 onglets (Informations, Évènements, Commandes et billets, Autorisations et consentements, Historique, Sécurité et RGPD), style `.org-tabs` déjà utilisé ailleurs dans l'admin.
- **Informations** : édition (prénom, nom, téléphone, téléphone secondaire, e-mail, date de naissance), validation stricte identique client/serveur (`lib/admin/clients/detail.ts`, testée) : formats de téléphone internationaux, date non future, e-mail unique, motif obligatoire (5 caractères mini) dès que l'e-mail ou la date de naissance change, alerte (non bloquante) si l'âge sort de 12–100 ans. Fenêtre de confirmation avant/après avant tout enregistrement (`/api/admin-clients/[id]/check`, aucune écriture). Concurrence : `expected_updated_at` → 409 si le compte a changé entre-temps.
- **Changement d'e-mail** : écrit d'abord dans `profiles` (audit avant/après), puis synchronisé comme identifiant de connexion via `supabase.auth.admin.updateUserById` ; échec de synchronisation journalisé (`customer.email_sync_failed`, action ajoutée à la liste blanche de `admin_customer_log`) et signalé à l'écran. L'ancienne adresse est prévenue par e-mail (Resend) ; échec → « notification non envoyée » journalisé (`customer.email_notice`), jamais bloquant.
- **Évènements** / **Commandes et billets** : À venir / Passés à partir des commandes réelles (référence, tarif, billets, statut incluant Gratuit, montant, lien vers la commande admin existante pour les commandes web). **Autorisations et consentements** : autorisation parentale, CGV acceptées (version), consentements nommés par commande — rien d'inventé, `Non renseigné`/« Aucune autorisation... » sinon.
- **Historique** : `audit_log` filtré sur le compte, avant/après lisible, motif affiché, nom de l'administrateur.
- **Sécurité et RGPD** : e-mail de réinitialisation du mot de passe (même parcours que « mot de passe oublié »), déconnexion de toutes les sessions, suspension/réactivation (motif obligatoire, bloque l'accès via `auth.users.banned_until`), export JSON du compte et anonymisation (double confirmation en retapant l'e-mail, refusée s'il reste un billet valide à venir, commandes conservées pour la comptabilité) réservés au **super-administrateur**.
- **Permissions déléguées** : `/admin/gestion/administrateurs` permet maintenant au super-admin de cocher `clients.lire` / `clients.modifier` par administrateur (désactivées par défaut) ; l'entrée de menu et toute la page Clients suivent ce droit, revérifié à chaque page et chaque route API (`_assert_clients` en base).
- **Bug trouvé et corrigé pendant les tests** : `app/admin/clients/loading.tsx` (ajouté au bloc 2 pour le squelette de la liste) enveloppait aussi la route imbriquée `/admin/clients/[id]` dans une frontière de streaming Next.js, transformant les `notFound()` de la fiche en « soft 404 » (statut 200 au lieu de 404) — exactement le piège déjà documenté dans `app/organisateur/layout.tsx`. Fichier supprimé ; le squelette de chargement de la liste reste géré côté client (`useTransition`, déjà en place).
- Tests : unitaires (validation du formulaire, 12–100 ans, motif obligatoire — 133 passent), SQL (027, tests de permissions et journal), Playwright `tests/e2e/s20.mjs` (404 page + 7 routes API pour tout non autorisé, ouverture avec dédup du journal, édition avec motif → entrée avant/après dans `audit_log`, e-mail synchronisé côté Auth, admin délégué `clients.modifier` peut éditer mais pas anonymiser/exporter, sécurité et RGPD complets, captures 1440/390 sans bande noire).
- Vérifié en ligne (la-sunshines.vercel.app) après le push : `/admin/clients/[id]` → 404 réel pour un visiteur non connecté (x-matched-path confirme que la route existe et rejette proprement, plus de soft-404), id mal formé → 404, chaque route API de sécurité/RGPD (password-reset, anonymize, export) → 404 sans session, aucune régression sur l'accueil, `/organisateur`, `/admin`.

### Clôture (2026-09-22)
Fenêtre de confirmation de la fiche passée en vraie boîte de dialogue modale (`showModal()`, piège du focus, `Échap`), pour la même accessibilité que la fenêtre d'export CSV — repéré en relisant l'exigence « accessible au clavier ». tsc + build (dossier séparé) + 135 tests unitaires + `test:db` + banc e2e complet (s3 à s20, toutes les sessions) : tous verts. Poussé et vérifié en ligne.
Non rejoué en Playwright (mais fonction SQL et contrôle d'accès testés directement) : le clic sur « Exporter en CSV » depuis le navigateur (ouverture de la fenêtre, avertissement RGPD, case « inclure les mineurs ») — la génération du CSV (BOM, anti-injection, en-têtes, exclusion des mineurs) est testée unitairement et la fonction `admin_customers_export` est testée en SQL.
**Reste à faire, uniquement à cause d'une restriction de la session** : la migration 027 n'a pas pu être appliquée sur la vraie base (les lectures/écritures directes sur la base de production ont été refusées par les permissions de l'environnement). Tant qu'elle n'est pas appliquée, `admin_clients_access` échoue proprement côté serveur : l'entrée « Clients » reste masquée dans le menu admin et `/admin/clients` répond 404 pour tout le monde, y compris le super-admin — le code est donc livré et vérifié mais **inerte** en production. Pour l'activer : appliquer `supabase/migrations/20260921000100_admin_clients.sql` sur la base réelle (après sauvegarde JSON), puis vérifier avec `supabase/tests/027_verify.sql`.
